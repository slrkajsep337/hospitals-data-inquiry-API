{{>layouts/header}}
<form action="/hospitals/search" method="get">
    주소 검색: <input type="text" name="keyword">
    <button type="submit" class="btn btn-primary">검색</button>
    <a>도로명 주소로 입력해주세요(예: 서울특별시 종로구 지봉로 50, 3~5층 (숭인동))</a>
</form>

<table class="table">
    <thead>
    <tr>
        <th scope="col">id</th>
        <th scope="col">병원 이름</th>
        <th scope="col">도로명 주소</th>
    </tr>
    </thead>
    <tbody class="table-group-divider">
    {{#hospitals}}
        <tr>
            <th scope="row">{{id}}</th>
            <td><a href="/hospitals/{{id}}">{{hospitalName}}</a></td>
            <td>{{roadNameAddress}}</td>
        </tr>
    {{/hospitals}}
    </tbody>
</table>
<ul class="pagination justify-content-center">
    {{#keyword}}
        <li class="page-item">
            <a class="page-link" href="?page={{previous}}&keyword={{keyword}}">Previous</a>
        </li>
        <li class="page-item">
            <a class="page-link" href="?page={{next}}&keyword={{keyword}}">Next</a>
        </li>
    {{/keyword}}
    {{^keyword}}
        <li class="page-item">
            <a class="page-link" href="?page={{previous}}">Previous</a>
        </li>
        <li class="page-item">
            <a class="page-link" href="?page={{next}}">Next</a>
        </li>
    {{/keyword}}
</ul>
{{>layouts/footer}}